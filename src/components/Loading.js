import {LoadingEl, LoadingHeading} from './Styled';

function Loading() {
    return (
        <LoadingEl>
            <LoadingHeading>Loading...</LoadingHeading>
        </LoadingEl>
    );
}

export default Loading;
